import LoadingWrapper from "@/components/LoadingWrapper/LoadingWrapper";

const Loading = () => {
  return (
    <main>
      <LoadingWrapper title="loading data" />
    </main>
  );
};

export default Loading;
