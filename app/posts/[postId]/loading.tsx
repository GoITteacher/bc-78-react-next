import LoadingWrapper from "@/components/LoadingWrapper/LoadingWrapper";

const Loading = () => {
  return (
    <main>
      <LoadingWrapper title="loading details" />
    </main>
  );
};

export default Loading;
